apiVersion: apps/v1
kind: Deployment
metadata:
  name: deadline-prediction
  namespace: manager-agent-ai
  labels:
    app: deadline-prediction
    version: v2.4.0
spec:
  replicas: 2
  selector:
    matchLabels:
      app: deadline-prediction
  template:
    metadata:
      labels:
        app: deadline-prediction
        version: v2.4.0
    spec:
      containers:
      - name: deadline-prediction
        image: manager-agent-ai-deadline-prediction:latest
        ports:
        - containerPort: 3009
        env:
        - name: NODE_ENV
          value: "production"
        - name: PORT
          value: "3009"
        - name: LOG_LEVEL
          value: "info"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: manager-agent-ai-secrets
              key: database-url
        - name: REDIS_URL
          valueFrom:
            secretKeyRef:
              name: manager-agent-ai-secrets
              key: redis-url
        resources:
          requests:
            memory: "512Mi"
            cpu: "200m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3009
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 3009
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        volumeMounts:
        - name: logs
          mountPath: /app/logs
      volumes:
      - name: logs
        emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: deadline-prediction-service
  namespace: manager-agent-ai
  labels:
    app: deadline-prediction
spec:
  selector:
    app: deadline-prediction
  ports:
  - port: 3009
    targetPort: 3009
    protocol: TCP
  type: ClusterIP
